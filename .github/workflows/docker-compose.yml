name: Сборка образа
on: 
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - name: Собираем образ
          uses: isbang/compose-action@v1.4.1
          with:
            compose-file: "./docker-compose.yml"
            down-flags: "--volumes"
            services: |
              db
              backend
